v1.x Â· apache 2.0
 
go Â· iPXE Â· sqlite/postgres

 PXE boot,
 
without the pain
.

Self-contained PXE and HTTP boot server. One binary. Zero config. Built-in proxyDHCP so you never touch your router. 50+ distros detected automatically.

$ get bootimus

view source
 
â

pts/0

bootimus â quickstart

# Pull, run, PXE boots. Thirty seconds.

$
 docker run -d --name bootimus \
    --cap-add NET_BIND_SERVICE \
    -p 67:67/udp -p 69:69/udp \
    -p 8080:8080/tcp -p 8081:8081/tcp \
    -v $(pwd)/data:/data \
    
garybowers/bootimus:latest

$
 docker logs bootimus | grep Password

admin: 
Password: 7f3a-plum-swift-echo

$
 open http://localhost:8081

[0] bash
*
 
1:vim
 
2:htop

"bootimus"
 
14:02

50+
distros detected
 
1
binary, zero deps
 
0
DHCP reconfigs
 
2
arch: amd64 Â· arm64

[01]
 
Single binary

Go binary with embedded iPXE, web UI, SQLite, and all assets. No runtime deps. Scp it and run.

[02]
 
Built-in proxyDHCP

Answers PXE on UDP/67 without touching your existing DHCP. Zero router reconfig. Drop in on any LAN.

[03]
 
50+ distros

Automatic kernel/initrd extraction for Ubuntu, Debian, Arch, Fedora, NixOS, Alpine, FreeBSD, Windows (wimboot), and more.

[04]
 
MAC-based ACL

Assign specific images per MAC. Auto-discover new clients on first PXE. Promote leases to static when ready.

[05]
 
One-click tools

GParted, Clonezilla, Memtest86+, SystemRescue, ShredOS, netboot.xyz. Enable from the UI, they show up in the menu.

[06]
 
JWT + LDAP

Token auth with bcrypt. Optional LDAP/AD backend with group-based admin. Local accounts stay as fallback.

[07]
 
REST API

Everything the UI does is an API call. Script boot assignments, scans, WOL triggers. Live log stream over SSE.

[08]
 
Runs anywhere

Multi-arch Docker (amd64/arm64), static binary, or a 2GB Alpine-based appliance image you can flash to USB.

[09]
 
Unattended installs

Drop autounattend.xml, kickstart, preseed, or cloud-init in. Attach to an image as the default, override per client. Bootimus stages it at boot â no clicks, no setup wizard.

pts/0

pxe boot trace â ubuntu-24.04

# Client boots, sends DHCPDISCOVER broadcast

[dhcp]
  
â
 DHCPDISCOVER from 
b4:2e:99:01:5f:a3
 (no PXE options from primary DHCP)

[proxy]
 
â
 DHCPOFFER-PXE: next-server=bootimus, filename=ipxe.efi

[tftp]
  
â
 RRQ ipxe.efi  
(198 KiB, 14 ms)

# iPXE loads, chains to HTTP menu

[http]
  
â
 GET /menu.ipxe  
200
  
2.1 KiB

[menu]
 
â³
 17 images Â· 3 groups Â· 6 tools

[menu]
 
â³
 user selected: 
ubuntu-24.04-live-server

# Kernel + initrd stream over HTTP

[http]
  
â
 GET /iso/ubuntu-24.04/vmlinuz  
200
  
14 MiB Â· 612 MB/s

[http]
  
â
 GET /iso/ubuntu-24.04/initrd   
200
  
76 MiB Â· 598 MB/s

[boot]
  
handoff ok
 Â· client booting

[0] bash
*
 
1:vim
 
2:htop

"bootimus"
 
14:02

â
Single Go binary
 Â· statically linked, ldd returns "not a dynamic executable". Reproducible builds from make release.
â
No proprietary blobs
 Â· embedded iPXE is upstream FOSS (GPL-2.0). No closed-source firmware shipped.
â
No telemetry, ever
 Â· zero call-home. Zero analytics. Zero "anonymous usage stats". Air-gapped LAN safe.
â
Apache 2.0
 Â· permissive licence. Use in commercial environments, ship internally, fork without strings.
â
Vendored deps, all FOSS
 Â· every transitive Go dependency is open source. go mod why any package.
â
Bring your own bootloader
 Â· don't trust the embedded iPXE? Drop your own signed binaries in. See below.

pts/0

bootimus version --verbose

$
 bootimus version --verbose

bootimus
      1.0.0

commit
       8e87824 (clean)

go
           1.23.4 linux/amd64

build
        static Â· reproducible

licence
      Apache-2.0

embedded

  ipxe        1.21.1+upstream  
GPL-2.0

  proprietary 
0 blobs

  telemetry   
disabled (compile-time)

$
 ldd ./bootimus

not a dynamic executable

$
 sha256sum ./bootimus

7f3a9b0câ¦
  bootimus

[0] bash
*
 
1:vim
 
2:htop

"bootimus"
 
14:02

iPXE Â· UEFI x86_64
 
ipxe.efi Â· the default. Built from upstream master, embedded in the binary.
 
embedded Â· fallback

iPXE Â· UEFI ARM64
 
ipxe-arm64.efi Â· for Raspberry Pi 4/5, Apple Silicon hosts, ARM servers.
 
embedded Â· fallback

iPXE Â· Legacy BIOS
 
undionly.kpxe Â· for old kit that won't UEFI. Still relevant in 2026.
 
embedded Â· fallback

Microsoft-signed shim
 
Drop a signed shimx64.efi + grubx64.efi in for Secure-Boot-enforced fleets. No firmware MOK enrolment needed.
 
custom Â· BYO

Custom-themed iPXE
 
Compile your own iPXE with branding, custom menu colours, embedded scripts. Drop the .efi in.
 
custom Â· BYO

GRUB / syslinux / pxelinux
 
Not iPXE? No problem. Anything that speaks TFTP and HTTP works. Bootimus just serves bytes.
 
custom Â· BYO

pts/0

bootloader sets â file fallthrough

$
 tree /var/lib/bootimus/bootloaders
data/bootloaders
âââ 
ipxe-builtin/
          
# embedded â always available

â   âââ ipxe.efi
â   âââ ipxe-arm64.efi
â   âââ undionly.kpxe
âââ 
ipxe-secureboot/
       
# your custom set

â   âââ shimx64.efi             
(signed by Microsoft)

â   âââ grubx64.efi             
(signed by Microsoft)

â   âââ ipxe.efi                
(signed by your CA)

âââ 
ipxe-themed/

    âââ ipxe.efi                
(custom branding)

$
 bootimus bootloaders use ipxe-secureboot

â
 active set: 
ipxe-secureboot

â
 falls back to 
ipxe-builtin
 for: 
ipxe-arm64.efi, undionly.kpxe

# PXE boot trace, Secure Boot client

[tftp]
 RRQ shimx64.efi      
â ipxe-secureboot/shimx64.efi

[tftp]
 RRQ ipxe-arm64.efi   
â ipxe-builtin/ipxe-arm64.efi (fallback)

[0] bash
*
 
1:vim
 
2:htop

"bootimus"
 
14:02

Ready to stop babysitting tftpd?
 
Docker, bare metal, or flashable USB. Pick your poison.

$ get bootimus
 
read the docs â
