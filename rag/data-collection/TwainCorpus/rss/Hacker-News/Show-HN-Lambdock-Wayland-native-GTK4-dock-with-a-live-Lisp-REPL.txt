jjba23
/
lambdock

Watch

			1

Fork

			0

 Code

 Issues

2

 Pull requests

 Activity

Modern, hyper-hackable, Wayland-native desktop dock application  (C and Guile Scheme + GTK4) w/ REPL

c

desktop

dock

gnu

gnu-linux

gtk

guile

linux

wayland

widget

252
 commits

2
 branches

27
 tags

3.3
 MiB

								C

						59.3%

								Scheme

						24.7%

								CSS

						11.3%

								Meson

						3.6%

								Nix

						0.7%

								Other

						0.4%

trunk

Find a file

		HTTPS

Download ZIP

Download TAR.GZ

Download BUNDLE

Open with VS Code

Open with VSCodium

Open with Intellij IDEA

		Repository files (latest commit first)

Filename

Latest commit message

Latest commit date

Josep Bigorra

bda6216efa

feat: 
✨
 Debian packaging improved

2026-08-12 10:53:29 +02:00

debian

feat: 
✨
 Debian packaging improved

2026-08-12 10:53:29 +02:00

protocols

feat: 
✨
 Add debian build

2026-08-04 08:54:07 +02:00

resources

feat: 
✨
 Improve CSS

2026-08-11 18:00:38 +02:00

scheme
/lambdock

fix: 
🔧
 REPL

2026-08-11 18:39:54 +02:00

src

feat: 
✨
 Improve icon resolution

2026-08-11 22:14:00 +02:00

.envrc

feat: 
✨
 CCLS

2026-07-30 10:58:55 +02:00

.gitattributes

feat: 
✨
 Finding icons

2026-07-30 15:40:45 +02:00

.gitignore

feat: 
✨
 Add debian build

2026-08-04 08:54:07 +02:00

channels.scm

feat: 
✨
 Improve GTK

2026-07-30 14:59:58 +02:00

COPYING

feat: 
✨
 Lambdock project

2026-07-30 10:52:05 +02:00

flake.lock

feat: 
✨
 Add Nix package

2026-08-06 15:51:33 +02:00

flake.nix

feat: 
✨
 Add Nix package

2026-08-06 15:51:33 +02:00

guix.scm

feat: 
✨
 Improve package definition

2026-08-10 11:39:13 +02:00

lambdock.spec

feat: 
✨
 Implement GResource loading

2026-08-05 10:41:04 +02:00

maak.scm

feat: 
✨
 Debian packaging improved

2026-08-12 10:53:29 +02:00

manifest.scm

feat: 
✨
 Improve icon resolution

2026-08-11 22:14:00 +02:00

meson.build

feat: 
✨
 Improve icon resolution

2026-08-11 22:17:31 +02:00

NEWS.org

fix: 
🔧
 REPL

2026-08-11 18:40:24 +02:00

package.nix

feat: 
✨
 Improve icon resolution

2026-08-11 22:17:31 +02:00

PKGBUILD

feat: 
✨
 Implement GResource loading

2026-08-05 10:41:04 +02:00

README.org

docs: 
📝
 README

2026-08-12 10:09:41 +02:00

README.org

lambdock

What systems does lambdock support?

Configuring lambdock

Why lambdock? A comparison table

Styling & Theming 
🎨

Finding icon names

Etymology

Interactive lambdock REPL

Wayland Toplevel Tracking & Popovers

Bi-Directionality of C and Guile Scheme 
🔄

Development & Building

Environment Setup with GNU Guix

Building with Meson

Building for Debian

Building for Fedora/openSUSE

Building for Arch

Building with Nix

Hacking with 
maak
 & CCLS

Animations

Licensing

AI Policy

Code of conduct

Why Lisp (GNU Guile Scheme) ? 
🔮

On Reproducibility, and Why GNU Guix? 
🐂
️

Backlog and Ideas

The dock for Wayland (GNU/Linux) that you didn't know you needed (C and Guile Scheme + GTK4)

lambdock
 is a modern, hyper-hackable, Wayland-native desktop dock application (as made popular by CairoDock, macOS, Plank) engineered for GNU/Linux users, people that like docks, macOS refugees,  ricers, free software and Lisp enthusiasts, Wayland minimalists…

It's built in the C programming language and using GTK4 libraries and toolkit, 
gtk4-layer-shell
, Lisp ( GNU Guile Scheme) with fullruntime inspectability and configurability, hot-reload of config and of the GTK-CSS.

Whether you are running Niri,  Sway, Hyprland, River, or any 
wlroots
 / Layer Shell compliant compositor, 
lambdock
 brings clean application launching, dynamic Wayland foreign-toplevel window tracking, and smooth cubic-eased auto-hiding directly to your setup.

lambdock
 is a lightweight, Wayland-native desktop dock built with a high-performance C core, 
GTK4 Layer-Shell
, and dynamic configuration powered by 
GNU Guile Scheme
.

Wayland Native
: Built on GTK4 and 
gtk4-layer-shell
 for smooth edge anchoring, cubic-eased slide animations, and compositor integration.

Dynamic Window Tracking
: Uses 
zwlr_foreign_toplevel_manager_v1
 to display active app indicators, window titles, and popover HUDs for focusing or closing instances.

Multi-Monitor Flexibility
 
🖥️
: Span docks across all connected screens (
#:monitor 'all
) or lock them to specific output connectors (e.g., 
"DP-1"
).

Smooth Auto-Hide
: Keeps the dock out of your way with configurable reveal thresholds and fluid sliding animations.

Declarative Lisp Configurations
 
🔮
: Harness the power of GNU Guile Scheme for declarative dock setups, custom app launchers, separators, and options. Multiple config files and multiple docks are supported.

Smart and Safe Hot-Reloading
: Automatic file watcher (
GFileMonitor
) and REPL hook (
reload-dock!
) safely re-evaluate Scheme settings and GTK CSS without blocking the UI thread.

Live Interactive Socket REPL
 
📡
: Query state or alter dock parameters live over a Unix domain socket (
/tmp/lambdock-repl.sock
) via Emacs/Geiser, 
socat
, or 
ncat
.

Theme Engine
 
🎨
: Extensible with CSS, and ships with built-in themes, while supporting user extensions and overrides via 
~/.config/lambdock/styles.css
.

Flexible Icon Resolution
: Best-effort fallback pipeline resolving GTK theme icons, absolute paths, system pixmaps, and symbolic variants.

Async Command Launching
: Spawns binaries, scripts, and Flatpaks asynchronously using GLib process spawning without freezing the UI.

App Noise Filtering
: Flexible exclusion lists (
#:exclude-apps
) to prevent background utilities or specific application IDs from creating unwanted launchers.

Reproducible Builds
: Fully hermetic development environment and builds powered by GNU Guile, Meson, and GNU Guix.

Native fluid fish-eye
 magnification effect on hover (a la macOS) which is opt-in since some users might want less animations.

Dock Scrolling
: Seamless mouse wheel scrolling over dock items when the bar exceeds available display dimensions.

What systems does lambdock support?

lambdock
 is a GNU/Linux first utility.

The tool uses 
gtk4-layer-shell
 to render to the screen and positioning and for dock behavior.

That means it works well in Wayland compositors that support the  
wlr-layer-shell-unstable-v1
 protocol, including:

KDE Plasma (Wayland session)

Smithay-based compositors: Niri, COSMIC Desktop

wlroots-based compositors: Sway, Hyprland, River, Wayfire

Mir-based compositors

Note: GNOME (Mutter) currently not supported due to not implementing the protocol

Configuring lambdock

Configurations are written in GNU Guile Scheme, and user style overrides are written in CSS/GTK4.

lambdock
 will scan your  configuration Directory: 
$XDG_CONFIG_HOME/lambdock/
 (usually 
~/.config/lambdock/
) for 
settings*.scm
 files, effectively supporting multi-dock multi-config.

Single Configuration File: 
~/.config/lambdock/settings.scm

Multi-Instance Configuration Files: 
~/.config/lambdock/settings-1.scm
, 
~/.config/lambdock/settings-2.scm
, etc.

Styles and overrides
: 
$XDG_CONFIG_HOME/lambdock/styles.css
 (usually 
~/.config/lambdock/styles.css
).

Hot-Reloading
: Save your files, and 
lambdock
 automatically updates existing docks or spawns/destroys dock instances as files are created or deleted.

Sensible Defaults:
 Every option comes pre-configured—you only need to override and export the settings you wish to customize.

Presets
 that can help you reduce boilerplate, see 
/jjba23/lambdock/src/branch/trunk/scheme/lambdock/presets.scm

;; Enable/disable smooth edge sliding auto-hide

(
define 
dock-auto-hide?
 
#t
)

;; Show dock when last window was closed

(
define 
dock-show-when-clear?
 
#t
)

;; Dock edge attachment: 'bottom | 'top | 'left | 'right

(
define 
dock-position
 
'bottom
)

;; Default icon pixel resolution

(
define 
dock-icon-size
 
48
)

;; Themes:

;; 'vanilla | 'vanilla-light | 'snow-leopard | 'solarized-light

;; 'nature | 'unity | 'none

(
define 
dock-theme
 
'vanilla
)

;; 'all         -> Render dock on every connected monitor

;; "DP-1"       -> Target specific monitor output connector

;; 'default     -> Default compositor monitor assignment

(
define 
dock-monitor
 
'all
)

;; List of app ids to never show in the dock

(
define 
dock-exclude-apps
 
'
())

;; Whether to enable the lambdock REPL

(
define 
dock-enable-repl?
 
#f
)

;; Location for the REPL unix socket

(
define 
dock-repl-socket-path

"/tmp/lambdock-repl.sock"
)

;; Whether to animate dock items when hovering on them with the mouse

(
define 
dock-hover-animate?
 
#t
)

;; Whether to keep track of open apps and windows and show indicators and popovers

(
define 
dock-track-windows?
 
#t
)

;; Application launchers list

(
define 
dock-items

(
list 
(
preset-launcher
 
'alacritty
)

(
preset-launcher
 
'google-chrome-flatpak
)

(
app-item
 
#
:name
 
"Spotify"

#
:exec
 
"flatpak run com.spotify.Client"

#
:icon
 
"com.spotify.Client"
)

(
app-item
 
#
:name
 
"Files"

#
:exec
 
"nautilus -w"

#
:icon
 
(
preset-icon
 
'nautilus
))

(
separator-item
)

(
app-item
 
#
:name
 
"Emacs"

#
:exec
 
"emacsclient -c"

#
:icon
 
"emacs"
)))

Advanced Functional Setup (Batch Presets)

;; Alternatively, construct dock-items procedurally using list operations:

(
define 
dock-items

(
append 
(
preset-launchers-for
 
'alacritty

'google-chrome-flatpak

'spotify

'nautilus

'nyxt
)

(
list 
(
separator-item
))

(
preset-launchers-for
 
'emacs

'intellij-flatpak

'bruno
)

(
list 
(
separator-item
))

(
preset-launchers-for
 
'btop

'pavucontrol

'onepassword
)))

Since lambdock configuration files are evaluated directly as standard GNU Guile Scheme, you can run any arbitrary Guile code right inside 
~/.config/lambdock/settings*.scm
. You can import modules, define helper functions, query system state (like time, hostnames, or installed packages), do API calls, and dynamically build your configuration.

An example 
~/.config/lambdock/settings.scm
  could look like:

(
use-modules
 
(
ice-9
 
popen
)

(
ice-9
 
rdelim
)

(
srfi
 
srfi-19
)

(
srfi
 
srfi-1
))

;; Helper to run a shell command and return its output as a clean string

(
define 
(
sh-output
 
cmd
)

(
let* 
((
port
 
(
open-input-pipe
 
cmd
))

(
output
 
(
read-line
 
port
)))

(
close-pipe
 
port
)

(
if 
(
eof-object? 
output
)
 
""
 
output
)))

;; Enable auto-hide only on a specific hostname (e.g., laptop setup)

(
define 
dock-auto-hide?

(
string=? 
(
string-trim-both
 
(
sh-output
 
"hostname"
))
 
"thinkpad"
))

Available Item Constructors

Scheme Constructor / Procedure

Return Type

Description

Keyword Arguments

(app-item ...)

Record

Custom application launcher definition

#:name
, 
#:exec
, 
#:icon

(dynamic-item ...)

Record

Dynamic polling widget displaying dynamic textual data

#:name
, 
#:icon
, 
#:exec
, 
#:poll-fn
, 
#:interval-ms
, 
#:hover-animate?

(preset-launcher 'symbol)

Record

Standard launcher resolved from internal preset list

Symbol (e.g. 
'emacs
, 
'alacritty
)

(preset-launchers-for 'a 'b)

List

Batch helper returning a list of preset launchers

Variadic list of symbols

(separator-item)

Record

Layout divider line

None

(preset-icon 'symbol)

String

Resolves default icon name string for a given preset

Symbol

Multi-Config & Multi-Dock Instance

lambdock
 features built-in multi-dock orchestration. Rather than being limited to a single dock bar, you can run multiple independent docks simultaneously across different screen edges or monitors.

Automatic Directory Monitoring
: 
lambdock
 watches 
~/.config/lambdock/
 for files matching 
settings.scm
 or 
settings-*.scm
 (e.g., 
settings-left.scm
, 
settings-bottom.scm
).

Each configuration file defines its own isolated 
LambdockState
, position (
dock-position
), theme (
dock-theme
), monitor targets (
dock-monitor
), and item launcher layout (
dock-items
).

Dynamic Spawning
: Creating a new 
settings-2.scm
 file instantly spawns a new dock bar on screen.

Dynamic Hot-Reloading
: Editing any 
settings-*.scm
 file hot-reloads that specific dock instance without flickering or restarting other running instances.

Dynamic Destruction
: Deleting a 
settings-*.scm
 file safely tears down and destroys its corresponding dock window, removing Wayland handles and GTK widgets without crashing the application.

Dock Scrolling & Viewport

When a dock contains numerous pinned launchers, running app windows, and dynamic widgets that exceed the available display width or height:

GTK Scrolled Viewport
: 
lambdock
 automatically encapsulates dock items inside an overflow-visible scrolled viewport container (
GtkScrolledWindow
).

Mouse Wheel Gestures
: Scrolling your mouse wheel or touchpad over the dock bar translates input deltas smoothly into axis movement along horizontal or vertical docks.

Why lambdock? A comparison table

While tools like Waybar and EWW excel as status bars or custom widget dashboards, 
lambdock
 is specifically engineered as a hyper-hackable, Wayland-native high-performance application dock with an embedded Lisp engine and full GTK4 graphics core.

Feature / Dimension

lambdock

Cairo-Dock / Plank

Waybar

EWW / Bar Engines

macOS Dock

Primary Design Target

Application Dock

Application Dock

Top/Bottom Bar

Widget Dashboard

Application Dock

Wayland Native (
wlr-layer-shell
)

✅

❌
 (X11 / WNCK)

✅

✅

(macOS Quartz)

UI Toolkit & Graphics Core

GTK4 (GL / GSK)

GTK2 / GTK3

GTK3

GTK3 / Cairo

Cocoa / Metal

Config & Scripting Engine

GNU Guile Scheme (Lisp)

Static INI / C

JSON / JSONC

yuck (Custom DSL)

Static plist

Multi-Dock Instances (Files)

✅
 Dynamic (
settings-*.scm
)

⚠️
 Limited

⚠️
 Multi-bar config

⚠️
 Multi-bar config

❌
 Single dock

Live Interactive Socket REPL

✅
 (Emacs/Geiser, 
socat
)

❌

❌

❌

❌

Hot-Reloading

✅
 Runtime Code, Docks & CSS

❌

⚠️
 Config/CSS

✅
 Config/CSS

❌
 (
killall Dock
)

Thread-Safe Async Dispatch

✅
 GLib 
g_idle_add

❌

⚠️
 Thread sync

⚠️
 IPC daemon

✅
 GCD / Dispatch

Wayland Window Tracking

✅
 
zwlr_foreign_toplevel_v1

❌
 (X11 only)

⚠️
 Taskbar module

❌
 (External scripts)

(macOS Server)

Multi-Instance Window HUD

✅
 Focus / Close / Close-All

⚠️
 Basic menu

⚠️
 Basic menu

❌
 Manual IPC

⚠️
 Basic menu

Dynamic Polling Widgets

✅
 Scheme Thunks (RAM/CPU/Clock)

⚠️
 C Plugins

✅
 Custom modules

✅
 Yuck pollers

❌
 Static

Hover Magnification (Fish-eye)

✅
 Cosine-Bell (Per-Item Toggle)

✅
 (X11 composite)

❌

❌

✅
 Native

Dock Viewport Scrolling

✅
 Mouse Wheel / Touchpad

❌

❌

❌

❌

Smooth Auto-Hide Animations

✅
 Frame-Clock Eased Slide

⚠️
 Basic

⚠️
 Basic

⚠️
 CSS transitions

✅
 Native

Metaprogramming & Macros

✅
 S-Exprs & Quasiquoting

❌

❌

⚠️
 Limited yuck

❌

Hermetic Guix & Nix Packaging

✅
 
guix.scm
 + 
flake.nix

❌

⚠️
 Distro packages

⚠️
 Distro packages

❌
 Proprietary OS

Preset Launcher Builders

✅
 
preset-launchers-for

❌

❌

❌

❌
 Drag-and-drop

Here a small demo of hot-reload config feature:

Styling & Theming 
🎨

lambdock
 ships with pre-baked themes embedded via build defines:

Vanilla Glassmorphism 
🧊
 (
'vanilla
): Modern translucent dark pill layout with soft backdrop reflections, glow indicators, and spring physics button effects.

Vanilla Light Glassmorphism 
🧊
 (
'vanilla-light
): Modern frosted glass translucent pill layout with soft backdrop reflections, glow indicators, and spring physics button effects.

Snow Leopard 3D Glass 
🍎
  (
'snow-leopard
): Classic 3D glossy shelf aesthetics with reflective top bevels, ice-blue indicator dots, and vertical lift displacement.

Solarized Light 
☀️
 (
'solarized-light
): Timeless palette, easy on the eyes, ivory, roasted, off-white goodness.

Nature 
🍀
 (
'nature
): deep organic woodland green gradients, warm leaf-tinted specular borders, and a lively Emerald Leaf glowing running app indicator.

Unity 
🪘
 (
'unity
): signature Yaru/Canonical design language: rich dark aubergine glass background, Ubuntu warm orange highlights, and an active indicator glowing in iconic Orange.

None (
'none
): no styling applied, using default GTK appearance.

You can override any GTK style class on the fly without re-compiling C sources. 
lambdock
 automatically monitors and loads user styles from:

~/.config/lambdock/styles.css

User styles are loaded with 
GTK_STYLE_PROVIDER_PRIORITY_USER
 priority, taking precedence over default app themes.

Make sure you use 
GTK CSS
, not "web-browser CSS". For understanding better what is available and what you can override, you should look at the theme definitions at 
./resources/css
.

An example override of a theme could look like this:

/* Custom user tweaks in ~/.config/lambdock/styles.css */

.
lambdock-window
 
{

background-image
:
 
linear-gradient
(
180
deg
,
 
rgba
(
255
,
 
0
,
 
20
,
 
0.85
)
 
0
%
,
 
rgba
(
5
,
 
5
,
 
8
,
 
0.95
)
 
100
%
);

border
:
 
1
px
 
solid
 
rgba
(
255
,
 
0
,
 
0
,
 
0.3
);

box-shadow
:
 
0
 
10
px
 
30
px
 
rgba
(
0
,
 
0
,
 
0
,
 
0.8
);

}

Finding icon names

lambdock
 supports loading and specifying icons in different ways. It loads the active GTK 4 Icon theme and proceeds to load the icon.

When you pass an icon string to 
app-item
, the C runtime's 
create_image_for_icon
 function checks for the icon in this order:

Direct File Path: Checks if the string is a valid file path on disk.

Theme Icon: Queries GtkIconTheme for the icon name in your active GTK icon theme (e.g., Papirus, Adwaita, Yaru), otherwise through DConf.

System Pixmaps: Checks 
/usr/share/pixmaps/
.

Symbolic Fallback: Appends -symbolic to try a monochrome fallback.

Generic Fallback: Displays the standard application-x-executable icon if no match is found.

You can also find available icons already in many ways, see some examples below:

grep -R -i 
"^Icon="
 /usr/share/applications/

# or using ripgrep which is nicer

rg -i 
"^Icon="
 /usr/share/applications/

# or flatpaks

rg -i 
"^Icon="
 /var/lib/flatpak/exports/share/applications/*.desktop

# or (user) flatpaks

rg -i 
"^Icon="
 ~/.local/share/flatpak/exports/share/applications/*.desktop

# or icons from scalable SVG

find /usr/share/icons/Adwaita/ -name 
"*.svg"
 
|
 xargs -n1 basename 
|
 sed 
's/\.svg$//'
 
|
 sort -u

Etymology

The name 
lambdock
 is a play on words combining:

Ship Docks
 
⚓
 where containers and applications dock safely.

lambda λ
 expressions from functional programming & Lisp enlightenment.

Lambs
 
🐑
 (gentle, fluffy, lightweight, and clean).

Docks
 as desktop UI components (e.g., Plank, Cairo-Dock, macOS Dock).

lambdock
 adopts as  its project logo the  
Agnus Dei
: The Lamb of God carrying a cross and a red flag

You could say using this dock 
es casi una experiencia religiosa como la de 
Enrique Iglesias

Interactive lambdock REPL

For purposes of development, experimentation and live hackability, 
lambdock
 features an embedded GNU Guile Scheme runtime. You can enable a background Unix domain socket REPL to query or dynamically alter the running dock's state in real time.

In your 
~/.config/lambdock/settings.scm
 file, set 
#:enable-repl?
 to 
#t
. You can optionally customize the socket path using 
#:repl-socket-path
 (defaults to 
/tmp/lambdock-repl.sock
):

(
configure-dock!

#
:position
 
'bottom

#
:items
 
(
list 
...
)

;; .... other settings

#
:enable-repl?
 
#t

#
:repl-socket-path
 
"/tmp/lambdock-repl.sock"
)

You can then inspect or modify the dock state, and when desired also reload the dock (with 
reload-dock!
).

Once connected, you can evaluate Scheme expressions against the running 
lambdock
 instance, possibilities are endless, e.g.

scheme@(guile-user)> (use-modules (lambdock core))
scheme@(guile-user)> (get-dock-theme) ;; -> 'vanilla
scheme@(guile-user)> (set-dock-theme! 'nature)
scheme@(guile-user)> (reload-dock!)

Emacs (Geiser) — Recommended

If you use Emacs with Geiser:

Run 
M-x geiser-connect-local
.

Select 
guile
 as the Scheme implementation.

Enter the socket path: 
/tmp/lambdock-repl.sock
.

Other options

You can connect directly from your terminal using different tooling, like 
nc
, 
ncat
,  
socat
 wrapped with 
rlwrap
 , and others:

socat - UNIX-CONNECT:/tmp/lambdock-repl.sock
ncat -U /tmp/lambdock-repl.sock
nc -U /tmp/lambdock-repl.sock
rlwrap socat - UNIX-CONNECT:/tmp/lambdock-repl.sock

Wayland Toplevel Tracking & Popovers

lambdock
 listens to 
zwlr_foreign_toplevel_manager_v1
 protocol events directly from your Wayland compositor (Sway, Hyprland, Wayfire, River, etc.):

Single Window: Left-clicking a running app launcher focuses and raises its existing window surface.

Multiple Windows: Left-clicking or right-clicking an app launcher with multiple open instances opens a floating GTK Popover HUD.

Popover Capabilities: Select individual windows by title, close specific window instances, or execute a "Close All Windows" command.

Bi-Directionality of C and Guile Scheme 
🔄

lambdock
 is not merely configured by Scheme—it is an embedded, extensible Lisp runtime environment wrapped around a high-performance C graphics engine. Execution flows bi-directionally between C and Guile.

  ┌────────────────────────────────────────────────────────────────────────┐
  │                           C RUNTIME (GTK4)                             │
  │                                                                        │
  │   main() ────► scm_boot_guile() ───► setup_guile_primitives()           │
  │                                           │ (Exports C Subrs)          │
  │                                           ▼                            │
  │   GFileMonitor / REPL Event  ◄─── (reload-dock!)                       │
  │           │                               │                            │
  │           ▼                               │ Thread-safe                │
  │   reload_dock_instance() ◄────────────────┘ g_idle_add()                │
  │           │                                                            │
  └───────────┼────────────────────────────────────────────────────────────┘
              │                               ▲
              │ Calls Scheme Procedures       │ Invokes C Primitives
              ▼                               │
  ┌───────────────────────────────────────────┴────────────────────────────┐
  │                         GUILE SCHEME ENGINE                            │
  │                                                                        │
  │   (lambdock core) ──► (init-dock!) ──► (start-repl!)                   │
  │   (lambdock state) ──► Getters / Setters ──► Socket REPL Thread        │
  └────────────────────────────────────────────────────────────────────────┘

C → Guile (Embedding & Inspection)

The entry point of the binary (
main.c
) boots the Guile interpreter using 
scm_boot_guile
. The C runtime acts as the host and invokes Scheme procedures to manage state and extract settings:

Module Loading
: C calls 
(init-dock!)
 via 
scm_c_public_ref
 to load and evaluate the user's 
~/.config/lambdock/settings.scm
.

Config Extraction
: Functions in 
guile_bridge.c
 inspect Scheme records and Lisp lists directly (
scm_call_0
, 
scm_to_utf8_string
, 
scm_to_bool
) to retrieve properties like 
get-dock-position
, 
get-dock-theme
, and 
get-dock-items
.

Socket REPL Spawning
: C calls 
(start-repl!)
, which uses Guile's 
call-with-new-thread
 and 
(system repl server)
 to spin up a background Unix domain socket thread.

Guile → C (Extending & Primitives)

Scheme is not restricted to passive data declarations, as it can trigger actions inside the running C engine:

C Primitives Export
: During initialization, 
setup_guile_primitives()
 uses 
scm_c_define_gsubr
 to register native C functions as Scheme procedures inside the 
(lambdock core)
 module.

Live REPL Reloads
: Evaluating 
(reload-dock!)
 from the Scheme REPL directly calls the C function 
scm_reload_dock
.

Thread-Safe Async Dispatch (
g_idle_add
)

Because the socket REPL server runs on a background Guile thread while GTK4 requires all UI updates to occur on the main thread, calling GTK primitives directly from Scheme would cause thread panics.

To solve this, 
lambdock
 bridges thread boundaries using GLib's 
g_idle_add
 event loop dispatcher.

For example, when 
(reload-dock!)
 is called over the socket REPL, C safely schedules the UI teardown and rebuild onto the GTK main loop without blocking or crashing the application.

Development & Building

The project requires the following C and Scheme dependencies:

GTK 4
 (
gtk4
)

GTK4 Layer Shell
 (
gtk4-layer-shell
)

GNU Guile 3.0
 (
guile-3.0
)

Meson
 & 
Ninja

pkg-config

Environment Setup with GNU Guix

Enter a development container with all required dependencies:

guix time-machine --channels
=
channels.scm -- shell -m manifest.scm

Also, a 
guix.scm
 is provided to run lambdock fully with Guix.

guix time-machine --channels
=
channels.scm -- shell -f guix.scm -- lambdock

Building with Meson

# Setup build directory

meson setup build

# Compile project

meson compile -C build

# Run locally

./build/lambdock

Building for Debian

Install the required build tools and compile the package:

# Install build dependencies on your Debian/Ubuntu system

sudo apt update
sudo apt install build-essential debhelper devscripts meson ninja-build 
\

                 pkg-config libgtk-4-dev libgtk4-layer-shell-dev 
\

                 guile-3.0-dev libwayland-dev wayland-protocols

# Build the package (unsigned binary build)

debuild -b -uc -us

Building for Fedora/openSUSE

Work in progress 
🚧
 see 
lambdock.spec

Building for Arch

Work in progress 
🚧
 see 
PKGBUILD

Building with Nix

lambdock can easily be used with Nix (flakes) too:

nix --extra-experimental-features 
'nix-command flakes'
 build
nix --extra-experimental-features 
'nix-command flakes'
 run .#

Hacking with 
maak
 & CCLS

If you use  
ccls
 or another LSP client, generate your local 
.ccls
 configuration using 
maak
. Otherwise read that code a bit and you can copy the solution.

# Generate .ccls with correct Guix package flags

maak add-libs

# Run lambdock inside time-machine environment

maak run

Animations

There are several approaches and layers to getting animations and smooth visual effects on the dock.

Lambdock features a smooth, frame-clock-driven auto-hide mechanism powered by modern GTK4 APIs and the Wayland Layer Shell protocol.

Starting with the auto-hide feature, lambdock features a cubic ease-out spatial transition to give a smooth showing and hiding to screen edge (within 250ms), and synchronizes with display refresh rates (60 Hz, 120 Hz, etc.) using 
gtk_widget_add_tick_callback
.

Also the opt-in fish-eye effect  (
dock-hover-animate?
) uses a cosine bell curve scaling for smooth zooming and sizing.

Of course, you are also free to add CSS 
transform
 directives or other things to your liking, e.g.

button
.
flat
:
hover
 
{

transform
:
 
scale
(
1.15
)
 
translateY
(
-4
px
);

}

Licensing

Lambdock and all of its source code are free software, licensed under the GNU General Public License v3 (or newer at your convenience).

https://www.gnu.org/licenses/gpl-3.0.html

The documentation and examples, including this document, which are provided with Lambdock, are all licensed under the GNU Free Documentation License v1.3 (or newer at your convenience).

https://www.gnu.org/licenses/fdl-1.3.html

AI Policy

This project adheres to the 
jointhefreeworld AI (Artificial Intelligence) policy
.

Our core principle is simple: 
AI should assist human creativity and problem-solving, never replace human reasoning.

While tools like Large Language Models (LLMs) and interactive chatbots can be beneficial for reviewing, refactoring small functions, or acting as a sounding board, they should be used with moderation.

We require a human in the loop for all contributions. The use of autonomous AI agents to automatically generate and submit pull requests to this project is strictly prohibited.

Code of conduct

This project adheres to the jointhefreeworld code of conduct. Find it here:

https://jointhefreeworld.org/blog/articles/personal/jointhefreeworld-code-of-conduct/index.html

In summary, we foster an inclusive, respectful, and cooperative environment for all contributors and users of this free software project. Inspired by the ideals of the GNU Project, we strive to uphold freedom, equality, and community as guiding principles. We believe that collaboration in a community of mutual respect is essential to creating excellent free software.

Why Lisp (GNU Guile Scheme) ? 
🔮

Desktop docks traditionally rely on static configuration formats like JSON, INI, or YAML. While straightforward, these formats force a compromise: adding dynamic behavior or custom logic requires writing C plugins or re-compiling the binary.

lambdock
 uses 
GNU Guile Scheme
 because desktop configuration shouldn't be limited to static key-value pairs. It should be an active, extensible programming environment.

⚡
 Live Interactive REPL & Inspectability

Lisp is built around live interactive development. With Guile embedded directly inside the C graphics core, you can connect a Unix domain socket REPL (via Emacs/Geiser or terminal tools like 
socat
) directly into the running dock process.
With 
lambdock
 you can inspect state variables, swap themes, dynamically insert launchers, or execute arbitrary Scheme functions on the fly, all without restarting the application or dropping Wayland frames.

🔮
 Power of Lisp

Clean and powerful  abstractions to batch-generate config dynamically and without boilerplate.

S-Expression Templating:
 Use meta-programming and quasiquoting and macro expansion for Lisp super powers

Code that manipulates and emits other code, write Lisp from Lisp, for my use-case in a deep system integration with my OS, (as demonstrated in my 
Heks GNU/Linux lambdock config
 ).

🔄
 Bi-Directional C / Guile Interop

Guile is engineered from the ground up as the official GNU extension language. Through the 
libguile
 API, execution flows seamlessly in both directions. This gives many interesting advantages.

On Reproducibility, and Why GNU Guix? 
🐂
️

A build should never fail because "it works on my machine." Software must aim to be transparent, fluid, powerful, and extensible, and for this, reproducibility is essential.

With 
GNU Guix
, you get:

🧠
 
Infinite Expressiveness:
 
Guile Scheme
 provides total, unconstrained control over packages and build definitions.

🎯
 
Bit-Identical Environments:
 Every dependency graph is declarative, immutable, and functionally derived.

⏳
 
Time Travel:
 Recreate the exact system state from months or years ago using 
guix time-machine
.

✨
 
No Hidden State:
 Build artifacts are pure functions of their inputs—zero implicit system dependencies.

🔍
 
Total Inspectability:
 Easily inspect, modify, and patch any point in the build graph.

Far from rigid, absolute reproducibility empowers fearless experimentation:

Declarative Shells:
 Instantly spin up isolated environments:

guix shell -m manifest.scm

🔄
 
Transactional Upgrades & Rollbacks:
 Experiment freely. If an update breaks, rollbacks are atomic and instantaneous.

Unified Tooling:
 One tool seamlessly handles dev shells, containers, system configs, and package builds.

Backlog and Ideas

TODO: Add some name mapping (e.g. org.gnome.SystemMonitor -> System Monitor) and localized?

TODO: Implement XDG desktop entry cache reader to inspect 
/usr/share/applications/*.desktop
 and 
~/.local/share/applications/*.desktop
 and flatpaks for  Flawless 
app_id
 Matching

find_dock_item_by_app_id
 relies on string matching heuristics between 
app_id
, 
icon
, 
name
, and 
exec
. This can occasionally fail when Wayland compositors emit generic or reverse-DNS names (e.g., 
org.gnome.Nautilus
 vs 
nautilus
).
Read 
StartupWMClass=
, 
Exec=
, and 
Icon=
 keys to map compositor ~app_id~x

TODO: Add more themes (CairoDock inspiration, Plank, other macOS inspired etc.)

WIP Finish Guix package and upstream it

WIP Finish Nix package and upstream it

TODO: Finish Debian package and publish it

TODO: Finish Fedora package and publish it

TODO: Finish Arch package and publish it

You can find the 
NEWS file with the Changelog here
